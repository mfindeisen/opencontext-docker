upstream open_context {
    server web:8000;
}

server {

    listen 90;

    location / {
        proxy_pass http://open_context;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

}